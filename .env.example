# 必填：用于签名会话 cookie 与 prelogin nonce
SESSION_SECRET=change-me-to-a-long-random-string

# Supabase（服务端使用 service role key）
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY
SUPABASE_DB_SCHEMA=app

# 本地开发：开启模拟 SSO
MOCK_SSO=true
MOCK_SSO_JWT_SECRET=dev-mock-sso-secret

# 生产/真实 SSO：验证 privnode 返回的 token
# HS256：设置 SSO_JWT_SECRET
SSO_JWT_ALG=HS256
SSO_JWT_SECRET=replace-with-real-sso-secret

# 提示：当 MOCK_SSO=true 时，会优先使用 MOCK_SSO_JWT_SECRET 验证 token（忽略 SSO_JWT_SECRET）。

# RS256：设置 SSO_JWT_PUBLIC_KEY_PEM（多行 PEM 可用 \n 转义）
# SSO_JWT_ALG=RS256
# SSO_JWT_PUBLIC_KEY_PEM="-----BEGIN PUBLIC KEY-----\n...\n-----END PUBLIC KEY-----"
